{% form_theme form 'user/_form_theme.html.twig' %}
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-lg btn-primary">{{ button_label|default('Create') }} <i class="fas fa-check"></i></button>
{{ form_end(form) }}
