{% extends 'home/username.html.twig'%}

{% block page_contents %}
    <div id="main" class="flex-full-center flex-vert">
        <h1>Welcome to {{block('title')}}</h1>
        <a href="{{ path('stack') }}">Analyse some images</a>
    </div>
{% endblock %}